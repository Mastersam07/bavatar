import 'package:flutter/material.dart';

class BavatarEmpty extends StatelessWidget {
  const BavatarEmpty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No Bavatars Found'),
    );
  }
}
